{
  "author": "Carles Capellas <capellas.carles@gmail.com>",
  "dependencies": {
    "@express-typed-api/server": "^1.0.7",
    "@sample-express-app/common": "file:../common",
    "express": "^4.18.2"
  },
  "description": "@express-typed-api/server usage samples",
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "license": "ISC",
  "name": "@sample-express-app/server",
  "private": true,
  "scripts": {
    "build": "../../../node_modules/.bin/tsc -p ./tsconfig.server.json",
    "build:check": "../../../node_modules/.bin/tsc -p ./tsconfig.server.json --noEmit",
    "clean": "rm -rf distribution",
    "clean-modules": "rm -rf node_modules",
    "dev": "nodemon source/sample-server.ts"
  },
  "version": "1.0.0"
}
